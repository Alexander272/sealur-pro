import React, { FC } from "react"
import { Container } from "../../../../../components/Container/Container"

type Props = {}

export const Moment: FC<Props> = () => {
    return <Container title='Расчет момента затяжки'>Moment</Container>
}
